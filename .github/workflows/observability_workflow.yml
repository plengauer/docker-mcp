name: OpenTelemetry
on:
  workflow_run:
    workflows:
      - Agentic Maintenance
      - Autobackport
      - Autotriage
      - Autoversion
      - Claude
      - CodeQL
      - Copilot
      - Copilot cloud agent
      - Copilot code review
      - Copilot setup steps
      - OpenAI Codex
      - Recompile Agentic Workflows
      - Renovate
      - ci
    types:
      - completed
jobs:
  export:
    runs-on: ubuntu-latest
    steps:
      - uses: plengauer/opentelemetry-github/actions/instrument/workflow@4b71071b2b044a176398118df98bb6b897a8458e # v5.61.4
        env:
          OTEL_EXPORTER_OTLP_ENDPOINT: ${{ secrets.OTEL_EXPORTER_OTLP_ENDPOINT }}
          OTEL_EXPORTER_OTLP_HEADERS: ${{ secrets.OTEL_EXPORTER_OTLP_HEADERS }}
    container: ghcr.io/plengauer/opentelemetry-github-workflow-instrumentation-runner:2169fb0fa9a8260fde99a2a185bbf99a5d63546e
    permissions:
      actions: read
